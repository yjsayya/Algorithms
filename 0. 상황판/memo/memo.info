-----------------------------------------------------------------------------
# 이런 에러가 났을 때;
ValueError: invalid literal for int() with base 10: ''
    리스트가 비어있는데 안에 있는 모든 걸 join하라고 하면 뜸
    li = []
    answer = "".join(li)


-----------------------------------------------------------------------------
<< python 내장함수 정리 >>
- python에 존재하는 내장함수는 총 31개라고 한다

1. map(func, iterable)
    - param2의 요소를 하나하나씩 param2의 함수를 적용해준다
    param1 - 함수
    param2 - iterable
    return - void(map 객체를 반환)

    ** return 값은 없기 때문에 list로 묶어주거나 혹은 tuple로 묶어줘야한다
        ex1) list(map(func, iterable)
        ex2) tuple(map(func, iterable)



eval()
enumerate()


----------------------------------------------------------------------
input() 대신 sys.stdin.readline()을 사용하는 이유
sys.stdin.readline()은 한줄을 단위로 입력 받기 때문에 --> 시간 복잡도 이득 볼 수 있음 ㅇㅋ
문법은 -->   "input() == sys.stdin.readline()"   이라고 보면 되겠다

import sys
a = int(sys.stdin.readline())



