# 역시 시간복잡도가 가장 작은 것은 대수학에 있는 공식을 이용하는 방법인 듯 

def solution(n):
    if a == b:
        return a
    else:
        return ((a+b) / 2) * (abs(a-b) + 1) 

    
    # return ((a+b) / 2) * (abs(a-b) + 1) if a != b else a
    # 이렇게 한 줄로 처리도 가능 